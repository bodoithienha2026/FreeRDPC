name: 🚀 SEVER AI STV PREMIUM (KAMI FIXED)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian sử dụng'
        required: false
        default: '5h40m'
        type: choice
        options:
        - '1h'
        - '3h' 
        - '5h40m'

jobs:
  Premium-RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      # 1. BANNER CHÀO MỪNG
      - name: 🎯 KHỞI ĐỘNG HỆ THỐNG
        run: |
          Write-Host "`n🤖 AI STV PREMIUM RDP SERVER" -ForegroundColor Yellow
          $frames = @('⣾', '⣽', '⣻', '⢿', '⡿', '⣟', '⣯', '⣷')
          for($i=0; $i -lt 10; $i++) {
              Write-Host "`r🚀 Đang khởi tạo... $($frames[$i % 8])" -NoNewline -ForegroundColor Blue
              Start-Sleep -Milliseconds 100
          }
          Write-Host "`r✅ Hệ thống đã sẵn sàng!                    " -ForegroundColor Green

      # 2. TẠO USER ADMIN & MỞ CỔNG RDP (Sửa lỗi 0x204)
      - name: 👤 THIẾT LẬP USER ADMIN
        run: |
          Write-Host "`n🛠️  ĐANG TẠO USER: Admin..." -ForegroundColor Yellow
          $name = "Admin"
          $pass = "window@123456"
          
          # Tạo User và cấp quyền Admin cao nhất
          net user $name $pass /add /expires:never /passwordchg:no
          net localgroup Administrators $name /add
          net localgroup "Remote Desktop Users" $name /add
          
          # Mở cổng RDP và cấu hình bảo mật để kết nối từ điện thoại/PC
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 1 -Force
          
          # Mở Firewall cổng 3389
          netsh advfirewall firewall add rule name="RDP-3389" dir=in action=allow protocol=TCP localport=3389 profile=any
          
          Write-Host "│ ✅ User: Admin | Pass: window@123456" -ForegroundColor Green

      # 3. KÍCH HOẠT KAMI TUNNEL (Sửa lệnh theo yêu cầu của bạn)
      - name: 🌐 KÍCH HOẠT KAMI TUNNEL
        run: |
          Write-Host "`n🌐 ĐANG THIẾT LẬP KAMI TUNNEL..." -ForegroundColor Yellow
          
          # Tải về với đúng tên file bạn muốn
          $url = "https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-windows-amd64.tar.gz"
          Invoke-WebRequest -Uri $url -OutFile "kami-tunnel-windows-amd64.tar.gz"
          
          # Lệnh giải nén đúng như bạn yêu cầu
          tar -xzf kami-tunnel-windows-amd64.tar.gz
          
          # Lệnh chạy đúng cổng 3389 như bạn yêu cầu
          Start-Process -FilePath ".\kami-tunnel.exe" -ArgumentList "3389" -NoNewWindow
          
          # Chờ lấy IP đầy đủ
          Write-Host "🔄 Đang lấy IP Public..." -NoNewline -ForegroundColor Cyan
          for($i=0; $i -lt 15; $i++) {
              if (Test-Path "kami_tunnel.txt") {
                  $ip = Get-Content "kami_tunnel.txt"
                  if ($ip -and $ip -match '\.') {
                      echo "KAMI_IP=$ip" >> $env:GITHUB_ENV
                      Write-Host "`r✅ Đã lấy được IP: $ip" -ForegroundColor Green
                      break
                  }
              }
              Write-Host "." -NoNewline -ForegroundColor Cyan
              Start-Sleep -Seconds 2
          }

      # 4. HIỂN THỊ THÔNG TIN ĐĂNG NHẬP
      - name: 🎉 THÔNG TIN KẾT NỐI
        run: |
          Write-Host "`n┌───────────────────────────────────────────┐" -ForegroundColor Cyan
          Write-Host "│              🎉 RDP ĐÃ SẴN SÀNG!                 │" -ForegroundColor Cyan
          Write-Host "├───────────────────────────────────────────┤" -ForegroundColor Cyan
          Write-Host "│ 🌐  IP Public  : $env:KAMI_IP" -ForegroundColor Yellow
          Write-Host "│ 👤  User       : Admin" -ForegroundColor White
          Write-Host "│ 🔐  Password   : window@123456" -ForegroundColor White
          Write-Host "└───────────────────────────────────────────┘`n" -ForegroundColor Cyan

      # 5. DUY TRÌ PHIÊN (TIMER)
      - name: ⏳ DUY TRÌ PHIÊN LÀM VIỆC
        run: |
          $totalMin = if("${{ github.event.inputs.duration }}" -eq "1h") {60} elseif("${{ github.event.inputs.duration }}" -eq "3h") {180} else {340}
          $endTime = (Get-Date).AddMinutes($totalMin)
          while ((Get-Date) -lt $endTime) {
              $left = $endTime - (Get-Date)
              Write-Host "`r💎 IP: $env:KAMI_IP | User: Admin | Còn lại: $($left.ToString('hh\:mm\:ss'))" -NoNewline -ForegroundColor Magenta
              Start-Sleep -Seconds 1
          }
