name: 🚀 SEVER AI STV PREMIUM (ADMIN KAMI)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian sử dụng'
        required: false
        default: '5h40m'
        type: choice
        options:
        - '1h'
        - '3h' 
        - '5h40m'

jobs:
  Premium-RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      # 1. BANNER CHÀO MỪNG
      - name: 🎯 KHỞI ĐỘNG HỆ THỐNG
        run: |
          Write-Host "`n🤖 AI STV PREMIUM RDP SERVER" -ForegroundColor Yellow
          Write-Host "🕒 Thời gian: ${{ github.event.inputs.duration }}`n" -ForegroundColor Magenta
          Write-Host "🚀 Đang khởi tạo hệ thống..." -ForegroundColor Blue
          Write-Host "✅ Hệ thống đã sẵn sàng!" -ForegroundColor Green

      # 2. TẠO USER ADMIN & MỞ CỔNG RDP (Mật khẩu: window@123456)
      - name: 👤 THIẾT LẬP USER ADMIN & RDP
        run: |
          Write-Host "`n🛠️  ĐANG TẠO USER: Admin..." -ForegroundColor Yellow
          
          # Tạo User Admin và cấp quyền (Pass: window@123456)
          net user Admin window@123456 /add /expires:never /passwordchg:no
          net localgroup Administrators Admin /add
          net localgroup "Remote Desktop Users" Admin /add
          
          # Mở cổng RDP trong Registry và Firewall
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-3389" dir=in action=allow protocol=TCP localport=3389 profile=any
          
          Write-Host "│ ✅ Đã tạo User Admin thành công!" -ForegroundColor Green
          Write-Host "│ ✅ Đã mở cổng RDP (3389)!" -ForegroundColor Green

      # 3. KÍCH HOẠT KAMI TUNNEL (Sử dụng đúng lệnh bạn yêu cầu)
      - name: 🌐 KÍCH HOẠT KAMI TUNNEL
        shell: cmd
        run: |
          echo.
          echo 🌐 ĐANG THIẾT LẬP KAMI TUNNEL...
          
          :: Tải Kami Tunnel
          curl -L -o kami-tunnel-windows-amd64.tar.gz https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-windows-amd64.tar.gz
          
          :: Giải nén đúng lệnh bạn yêu cầu
          tar -xzf kami-tunnel-windows-amd64.tar.gz
          
          :: Chạy đúng lệnh bạn yêu cầu (cổng 3389)
          start /b kami-tunnel.exe 3389

      # 4. LẤY IP VÀ HIỂN THỊ THÔNG TIN
      - name: 🎉 THÔNG TIN KẾT NỐI
        run: |
          Write-Host "`n🔄 Đang lấy IP Public từ Kami..." -ForegroundColor Cyan
          $ip = ""
          # Vòng lặp chờ IP hiện ra full
          for($i=0; $i -lt 15; $i++) {
              if (Test-Path "kami_tunnel.txt") {
                  $ip = Get-Content "kami_tunnel.txt"
                  if ($ip -and $ip -match '\.') {
                      echo "KAMI_IP=$ip" >> $env:GITHUB_ENV
                      break
                  }
              }
              Start-Sleep -Seconds 2
          }
          
          Write-Host "`n┌───────────────────────────────────────────┐" -ForegroundColor Cyan
          Write-Host "│              🎉 RDP ĐÃ SẴN SÀNG!                 │" -ForegroundColor Cyan
          Write-Host "├───────────────────────────────────────────┤" -ForegroundColor Cyan
          Write-Host "│ 🌐  IP Kết Nối : $ip" -ForegroundColor Yellow
          Write-Host "│ 👤  User       : Admin" -ForegroundColor White
          Write-Host "│ 🔐  Password   : window@123456" -ForegroundColor White
          Write-Host "└───────────────────────────────────────────┘`n" -ForegroundColor Cyan

      # 5. DUY TRÌ PHIÊN (TIMER)
      - name: ⏳ DUY TRÌ PHIÊN LÀM VIỆC
        run: |
          $totalMin = if("${{ github.event.inputs.duration }}" -eq "1h") {60} elseif("${{ github.event.inputs.duration }}" -eq "3h") {180} else {340}
          $endTime = (Get-Date).AddMinutes($totalMin)
          while ((Get-Date) -lt $endTime) {
              $left = $endTime - (Get-Date)
              Write-Host "`r💎 IP: $env:KAMI_IP | User: Admin | Còn lại: $($left.ToString('hh\:mm\:ss'))" -NoNewline -ForegroundColor Magenta
              Start-Sleep -Seconds 1
          }
