name: 🚀 SEVER AI STV PREMIUM (FIX KAMI TXT)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian sử dụng'
        required: false
        default: '5h40m'
        type: choice
        options:
        - '1h'
        - '3h' 
        - '5h40m'

jobs:
  Premium-RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      # 1. KHỞI ĐỘNG HỆ THỐNG
      - name: 🎯 KHỞI ĐỘNG HỆ THỐNG
        run: |
          Write-Host "🤖 AI STV PREMIUM RDP SERVER" -ForegroundColor Yellow
          Write-Host "✅ Hệ thống đã sẵn sàng!" -ForegroundColor Green

      # 2. TẠO USER ADMIN (Pass: window@123456)
      - name: 👤 THIẾT LẬP USER ADMIN
        run: |
          net user Admin window@123456 /add /expires:never /passwordchg:no
          net localgroup Administrators Admin /add
          net localgroup "Remote Desktop Users" Admin /add
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-3389" dir=in action=allow protocol=TCP localport=3389 profile=any

      # 3. KÍCH HOẠT KAMI TUNNEL (ĐÚNG LỆNH CỦA BẠN)
      - name: 🌐 KÍCH HOẠT KAMI TUNNEL
        shell: cmd
        run: |
          curl -L -o kami-tunnel-windows-amd64.tar.gz https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-windows-amd64.tar.gz
          tar -xzf kami-tunnel-windows-amd64.tar.gz
          :: Chạy ngầm để không treo log, dữ liệu IP sẽ tự xuất ra kami_tunnel.txt
          start /b kami-tunnel.exe 3389

      # 4. HIỂN THỊ THÔNG TIN (Đọc từ kami_tunnel.txt)
      - name: 🎉 THÔNG TIN KẾT NỐI
        run: |
          Write-Host "🔄 Đang chờ Kami xuất IP ra file kami_tunnel.txt..." -ForegroundColor Cyan
          $ip = ""
          # Đợi tối đa 30 giây để file txt có dữ liệu
          for($i=0; $i -lt 15; $i++) {
              if (Test-Path "kami_tunnel.txt") {
                  $ip = Get-Content "kami_tunnel.txt"
                  if ($ip -and $ip -match '\.') { break }
              }
              Start-Sleep -Seconds 2
          }
          
          Write-Host "`n┌───────────────────────────────────────────┐" -ForegroundColor Cyan
          Write-Host "│              🎉 RDP ĐÃ SẴN SÀNG!                 │" -ForegroundColor Cyan
          Write-Host "├───────────────────────────────────────────┤" -ForegroundColor Cyan
          Write-Host "│ 🌐  Địa chỉ IP : $ip" -ForegroundColor Yellow
          Write-Host "│ 👤  Tài khoản  : Admin" -ForegroundColor White
          Write-Host "│ 🔐  Mật khẩu   : window@123456" -ForegroundColor White
          Write-Host "│ 📍  Port       : 3389" -ForegroundColor White
          Write-Host "└───────────────────────────────────────────┘`n" -ForegroundColor Cyan

      # 5. DUY TRÌ PHIÊN (Giữ server sống)
      - name: ⏳ DUY TRÌ PHIÊN LÀM VIỆC
        run: |
          $endTime = (Get-Date).AddMinutes(340)
          while ((Get-Date) -lt $endTime) {
              $left = $endTime - (Get-Date)
              Write-Host "`r💎 Hệ thống đang chạy... Còn lại: $($left.ToString('hh\:mm\:ss'))" -NoNewline -ForegroundColor Magenta
              Start-Sleep -Seconds 10
          }
