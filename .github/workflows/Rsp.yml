name: 🚀 SEVER AI STV PREMIUM (NEW USER)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian sử dụng'
        required: false
        default: '5h40m'
        type: choice
        options:
        - '1h'
        - '3h' 
        - '5h40m'

jobs:
  Premium-RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      # 1. BANNER CHÀO MỪNG
      - name: 🎯 KHỞI ĐỘNG HỆ THỐNG
        run: |
          Write-Host "`n🤖 AI STV PREMIUM RDP SERVER" -ForegroundColor Yellow
          Write-Host "⚡ Powered by AI STV & Kami Tunnel" -ForegroundColor Green
          Write-Host "🕒 Thời gian: ${{ github.event.inputs.duration }}`n" -ForegroundColor Magenta
          $frames = @('⣾', '⣽', '⣻', '⢿', '⡿', '⣟', '⣯', '⣷')
          for($i=0; $i -lt 10; $i++) {
              Write-Host "`r🚀 Đang khởi tạo hệ thống... $($frames[$i % 8])" -NoNewline -ForegroundColor Blue
              Start-Sleep -Milliseconds 100
          }
          Write-Host "`r✅ Hệ thống đã sẵn sàng!                    " -ForegroundColor Green

      # 2. TẠO USER MỚI (KHÔNG PHẢI USER CHÍNH)
      - name: 👤 TẠO TÀI KHOẢN RIÊNG
        run: |
          Write-Host "`n🛠️  ĐANG TẠO USER MỚI..." -ForegroundColor Yellow
          $name = "AISTV-PREMIUM"
          $pass = "123456"
          
          # Tạo User mới
          net user $name $pass /add /expires:never
          
          # Thêm vào nhóm Administrators và Remote Desktop Users
          net localgroup Administrators $name /add
          net localgroup "Remote Desktop Users" $name /add
          
          # Cấu hình RDP không cần xác thực mạng (NLA)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          
          Write-Host "│ ✅ User: $name | Pass: $pass" -ForegroundColor Green
          Write-Host "🎯 Đã cấp quyền Admin và RDP cho User mới!" -ForegroundColor Green

      # 3. KẾT NỐI KAMI TUNNEL
      - name: 🌐 KÍCH HOẠT KAMI TUNNEL
        run: |
          Write-Host "`n🌐 ĐANG LẤY IP PUBLIC QUA KAMI..." -ForegroundColor Yellow
          Invoke-WebRequest -Uri "https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-windows-amd64.tar.gz" -OutFile "kami.tar.gz"
          tar -xzf kami.tar.gz
          
          # Chạy tunnel cổng 3389
          Start-Process -FilePath ".\kami-tunnel.exe" -ArgumentList "3389" -NoNewWindow
          
          # Đợi lấy IP từ file txt
          Start-Sleep -Seconds 8
          if (Test-Path "kami_tunnel.txt") {
              $ip = Get-Content "kami_tunnel.txt"
              echo "KAMI_IP=$ip" >> $env:GITHUB_ENV
              Write-Host "✅ Địa chỉ IP: $ip" -ForegroundColor Green
          }

      # 4. HIỂN THỊ THÔNG TIN
      - name: 🎉 THÔNG TIN KẾT NỐI
        run: |
          Write-Host "`n┌───────────────────────────────────────────┐" -ForegroundColor Cyan
          Write-Host "│              🎉 KẾT NỐI THÀNH CÔNG!              │" -ForegroundColor Cyan
          Write-Host "├───────────────────────────────────────────┤" -ForegroundColor Cyan
          Write-Host "│ 🌐  IP Public : $env:KAMI_IP" -ForegroundColor Yellow
          Write-Host "│ 👤  User       : AISTV-PREMIUM" -ForegroundColor White
          Write-Host "│ 🔐  Password   : 123456" -ForegroundColor White
          Write-Host "└───────────────────────────────────────────┘`n" -ForegroundColor Cyan

      # 5. DUY TRÌ PHIÊN (TIMER)
      - name: ⏳ DUY TRÌ PHIÊN LÀM VIỆC
        run: |
          $totalMin = if("${{ github.event.inputs.duration }}" -eq "1h") {60} elseif("${{ github.event.inputs.duration }}" -eq "3h") {180} else {340}
          $endTime = (Get-Date).AddMinutes($totalMin)
          Write-Host "🔄 Hệ thống đang chạy..." -ForegroundColor Yellow
          while ((Get-Date) -lt $endTime) {
              $left = $endTime - (Get-Date)
              Write-Host "`r💎 IP: $($env:KAMI_IP) | Còn lại: $($left.ToString('hh\:mm\:ss'))" -NoNewline -ForegroundColor Magenta
              Start-Sleep -Seconds 1
          }
