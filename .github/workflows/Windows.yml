name: 🖥️ REMOTE DESKTOP SERVICES

on: 
  workflow_dispatch:
    inputs:
      os_version:
        description: '📀 Select Operating System'
        required: true
        default: 'Windows Server 2025 (Native - 4vCPU | 16GB RAM)'
        type: choice
        options: 
          - "Windows Server 2025 (Native - 4vCPU | 16GB RAM)"
          - "Windows Server 2025 (Docker - 4vCPU | 8GB RAM)"
          - "Windows Server 2022 (Docker - 4vCPU | 8GB RAM)"
          - "Windows Server 2019 (Docker - 4vCPU | 8GB RAM)"
          - "Windows Server 2012 (Docker - 4vCPU | 8GB RAM)"
          - "Windows 11 Professional (Docker - 4vCPU | 8GB RAM)"
          - "Windows 10 Professional (Docker - 4vCPU | 8GB RAM)"
          - "Ubuntu 24.04 Desktop RDP (Native - 4vCPU | 16GB RAM)"

jobs:
  # ==================== JOB 1: WINDOWS SERVER 2025 NATIVE ====================
  windows-server-2025-native:
    if: github.event.inputs.os_version == 'Windows Server 2025 (Native - 4vCPU | 16GB RAM)'
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      - name: 🔧 Initializing Windows Server 2025 (Native)
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          
          # Create Administrator Account
          net user Admin Window@123456 /add /expires:never /passwordchg:no
          net localgroup Administrators Admin /add
          net localgroup "Remote Desktop Users" Admin /add
          
          # Configure RDP Service
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-3389" dir=in action=allow protocol=TCP localport=3389 profile=any
          
          # Create Information File
          $infoContent = @"
╔════════════════════════════════════════════════════════════╗
║           WINDOWS SERVER INFORMATION                        ║
╠════════════════════════════════════════════════════════════╣
║ Creator    : Zun                                           ║
║ Repository : https://github.com/zun209384-lgtm/window.git ║
║ System     : Windows Server 2025 (Native)                  ║
║ Resources  : 4 vCPU | 16GB RAM                             ║
║ Created    : $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')                    ║
╚════════════════════════════════════════════════════════════╝
"@
          Set-Content -Path "$env:USERPROFILE\Desktop\window.txt" -Value $infoContent -Encoding UTF8
          
          # Start Kami Tunnel
          Start-Process powershell -ArgumentList "-NoProfile -Command `
            iwr https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-windows-amd64.tar.gz -OutFile kami-tunnel-windows-amd64.tar.gz; `
            tar -xzf kami-tunnel-windows-amd64.tar.gz; `
            .\kami-tunnel.exe 3389"

      - name: 🌐 Connection Information - Windows Server 2025
        if: github.event.inputs.os_version == 'Windows Server 2025 (Native - 4vCPU | 16GB RAM)'
        run: |
          Write-Host "🔄 Establishing connection to Windows Server 2025..." -ForegroundColor Cyan
          $ip = ""
          while (-not $ip) {
              if (Test-Path "kami_tunnel.txt") {
                  $content = Get-Content "kami_tunnel.txt"
                  if ($content -match '\.') { $ip = $content }
              }
              if (-not $ip) { Start-Sleep -Seconds 3 }
          }
          
          Write-Host "`n╔════════════════════════════════════════════════════════════╗" -ForegroundColor Green
          Write-Host "║      ✅ WINDOWS SERVER 2025 - READY FOR CONNECTION        ║" -ForegroundColor Green
          Write-Host "╠════════════════════════════════════════════════════════════╣" -ForegroundColor White
          Write-Host "║ 🌐  Public IP   : $ip" -ForegroundColor Yellow
          Write-Host "║ 👤  Username    : Admin" -ForegroundColor White
          Write-Host "║ 🔐  Password    : Window@123456" -ForegroundColor White
          Write-Host "║ 📍  RDP Port    : 3389" -ForegroundColor White
          Write-Host "║ 💻  Resources   : 4 vCPU | 16GB RAM" -ForegroundColor Cyan
          Write-Host "║ 📄  Info File   : Desktop\window.txt" -ForegroundColor Magenta
          Write-Host "╚════════════════════════════════════════════════════════════╝`n" -ForegroundColor Green

      - name: ⏰ Maintaining Active Session
        if: github.event.inputs.os_version == 'Windows Server 2025 (Native - 4vCPU | 16GB RAM)'
        run: |
          $endTime = (Get-Date).AddMinutes(340)
          while ((Get-Date) -lt $endTime) {
              $left = $endTime - (Get-Date)
              Write-Host "`r🟢 Windows Server 2025 Active | Remaining: $($left.ToString('hh\:mm\:ss'))" -NoNewline -ForegroundColor Green
              Start-Sleep -Seconds 10
          }

  # ==================== JOB 2: WINDOWS WITH DOCKER ====================
  windows-docker:
    if: |
      github.event.inputs.os_version != 'Windows Server 2025 (Native - 4vCPU | 16GB RAM)' &&
      github.event.inputs.os_version != 'Ubuntu 24.04 Desktop RDP (Native - 4vCPU | 16GB RAM)'
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - name: 🔧 Initializing Windows System
        run: |
          sudo apt-get update > /dev/null 2>&1
          
          SELECTED_OS="${{ github.event.inputs.os_version }}"
          
          # Determine Windows Version
          if [[ "$SELECTED_OS" == *"2025"* ]]; then
            VERSION="2025"
            OS_NAME="Windows Server 2025"
          elif [[ "$SELECTED_OS" == *"2022"* ]]; then
            VERSION="2022"
            OS_NAME="Windows Server 2022"
          elif [[ "$SELECTED_OS" == *"2019"* ]]; then
            VERSION="2019"
            OS_NAME="Windows Server 2019"
          elif [[ "$SELECTED_OS" == *"2012"* ]]; then
            VERSION="2012"
            OS_NAME="Windows Server 2012"
          elif [[ "$SELECTED_OS" == *"11"* ]]; then
            VERSION="11"
            OS_NAME="Windows 11 Professional"
          elif [[ "$SELECTED_OS" == *"10"* ]]; then
            VERSION="10"
            OS_NAME="Windows 10 Professional"
          fi
          
          # Create Docker Compose Configuration
          cat <<EOF > docker-compose.yml
          version: "3.9"
          services:
            windows:
              image: dockurr/windows
              container_name: windows_rdp
              privileged: true
              environment:
                VERSION: "$VERSION"
                USERNAME: "Admin"
                PASSWORD: "Window@123456"
                RAM_SIZE: "8G"
                CPU_CORES: "4"
                DISK_SIZE: "60G"
              devices: [ "/dev/kvm" ]
              ports:
                - "3389:3389"
          EOF
          
          docker compose up -d > /dev/null 2>&1
          
          # Wait for Windows to initialize and create info file
          sleep 30
          docker exec windows_rdp powershell -Command "
            \$infoContent = @'
╔════════════════════════════════════════════════════════════╗
║           WINDOWS SERVER INFORMATION                        ║
╠════════════════════════════════════════════════════════════╣
║ Creator    : Zun                                           ║
║ Repository : https://github.com/zun209384-lgtm/window.git ║
║ System     : $OS_NAME (Docker)                             ║
║ Resources  : 4 vCPU | 8GB RAM                              ║
║ Created    : \$(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')    ║
╚════════════════════════════════════════════════════════════╝
'@
            Set-Content -Path 'C:\Users\Admin\Desktop\window.txt' -Value \$infoContent -Encoding UTF8
          " > /dev/null 2>&1
          
          echo "OS_NAME=$OS_NAME" >> $GITHUB_ENV
          
          # Setup Kami Tunnel
          wget -q https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-linux-amd64.tar.gz
          tar -xzf kami-tunnel-linux-amd64.tar.gz > /dev/null 2>&1
          chmod +x kami-tunnel
          ./kami-tunnel 3389 > /dev/null 2>&1 &

      - name: 🌐 Connection Information - Windows System
        run: |
          echo "🔄 Establishing connection to $OS_NAME..."
          
          while [ ! -f kami_tunnel.txt ]; do sleep 2; done
          
          RDP_IP=""
          while [ -z "$RDP_IP" ]; do
            RDP_IP=$(cat kami_tunnel.txt)
            sleep 2
          done

          echo ""
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║         ✅ $OS_NAME - READY FOR CONNECTION         ║"
          echo "╠════════════════════════════════════════════════════════════╣"
          echo "║ 🌐  Public IP   : $RDP_IP"
          echo "║ 👤  Username    : Admin"
          echo "║ 🔐  Password    : Window@123456"
          echo "║ 📍  RDP Port    : 3389"
          echo "║ 💻  Resources   : 4 vCPU | 8GB RAM"
          echo "║ 📄  Info File   : Desktop\window.txt"
          echo "╚════════════════════════════════════════════════════════════╝"
          echo ""
          echo "KAMI_IP=$RDP_IP" >> $GITHUB_ENV

      - name: ⏰ Maintaining Active Session
        run: |
          for i in {1..72}; do
            echo "🟢 [$(date +%H:%M:%S)] $OS_NAME Active | IP: $KAMI_IP"
            sleep 300
          done

  # ==================== JOB 3: UBUNTU DESKTOP RDP NATIVE ====================
  ubuntu-desktop-rdp:
    if: github.event.inputs.os_version == 'Ubuntu 24.04 Desktop RDP (Native - 4vCPU | 16GB RAM)'
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - name: 🔧 Initializing Ubuntu Desktop with RDP
        run: |
          # Update system
          sudo apt-get update > /dev/null 2>&1
          
          # Install XFCE Desktop Environment (lightweight)
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            xfce4 xfce4-goodies xorg dbus-x11 x11-xserver-utils > /dev/null 2>&1
          
          # Install XRDP Server
          sudo apt-get install -y xrdp > /dev/null 2>&1
          
          # Configure XRDP
          sudo systemctl enable xrdp > /dev/null 2>&1
          echo "xfce4-session" | tee ~/.xsession > /dev/null
          sudo systemctl restart xrdp > /dev/null 2>&1
          
          # Create Administrator Account
          sudo useradd -m -s /bin/bash Admin
          echo "Admin:Ubuntu@123456" | sudo chpasswd
          sudo usermod -aG sudo Admin
          
          # Configure XRDP for new user
          sudo -u Admin bash -c "echo 'xfce4-session' > /home/Admin/.xsession"
          
          # Create Information File
          sudo -u Admin bash -c 'cat > /home/Admin/Desktop/ubuntu-info.txt << "EOF"
╔════════════════════════════════════════════════════════════╗
║           UBUNTU DESKTOP INFORMATION                        ║
╠════════════════════════════════════════════════════════════╣
║ Creator    : Zun                                           ║
║ Repository : https://github.com/zun209384-lgtm/window.git ║
║ System     : Ubuntu 24.04 Desktop RDP (Native)             ║
║ Resources  : 4 vCPU | 16GB RAM                             ║
║ Desktop    : XFCE4                                          ║
║ Created    : '"$(date '+%Y-%m-%d %H:%M:%S')"'              ║
╚════════════════════════════════════════════════════════════╝
EOF'
          
          # Setup Kami Tunnel
          wget -q https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-linux-amd64.tar.gz
          tar -xzf kami-tunnel-linux-amd64.tar.gz > /dev/null 2>&1
          chmod +x kami-tunnel
          ./kami-tunnel 3389 > /dev/null 2>&1 &

      - name: 🌐 Connection Information - Ubuntu Desktop RDP
        run: |
          echo "🔄 Establishing connection to Ubuntu 24.04 Desktop..."
          
          while [ ! -f kami_tunnel.txt ]; do sleep 2; done
          
          RDP_IP=""
          while [ -z "$RDP_IP" ]; do
            RDP_IP=$(cat kami_tunnel.txt)
            sleep 2
          done

          echo ""
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║    ✅ UBUNTU 24.04 DESKTOP RDP - READY FOR CONNECTION     ║"
          echo "╠════════════════════════════════════════════════════════════╣"
          echo "║ 🌐  Public IP   : $RDP_IP"
          echo "║ 👤  Username    : Admin"
          echo "║ 🔐  Password    : Ubuntu@123456"
          echo "║ 📍  RDP Port    : 3389"
          echo "║ 💻  Resources   : 4 vCPU | 16GB RAM"
          echo "║ 🖥️  Desktop     : XFCE4"
          echo "║ 📄  Info File   : Desktop/ubuntu-info.txt"
          echo "╚════════════════════════════════════════════════════════════╝"
          echo ""
          echo "KAMI_IP=$RDP_IP" >> $GITHUB_ENV

      - name: ⏰ Maintaining Active Session
        run: |
          for i in {1..72}; do
            echo "🟢 [$(date +%H:%M:%S)] Ubuntu 24.04 Desktop RDP Active | IP: $KAMI_IP"
            sleep 300
          done
